sevenFacette:
  http:
    clients:
      restfulBooker:
        connectionTimeout: 5000
        connectionRequestTimeout: 4000
        socketTimeout: 3000
        url:
          baseUrl: localhost
          port: 3001
          protocol: http
        proxy:
          host: localhost
          port: 8080
        authentication:
          type: basic
          username: test
          password: testPass
      testClient:
        connectionTimeout: 5000
        connectionRequestTimeout: 4000
        socketTimeout: 3000
        url:
          baseUrl: localhost
          port: 3001
          protocol: http
